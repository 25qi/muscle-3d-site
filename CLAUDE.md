# 與使用者的協作規則

- **每次跑完使用者交代的指令(尤其是 build / commit / push)後,要發一個桌面通知(PushNotification 工具)告訴使用者已完成**,通知內文一句話講清楚做了什麼。除了通知外,仍在回覆文字裡簡短說明並附上推的 commit。不要讓使用者猜工作到底完成了沒。

- **非必要不要用破折號(—、–、——)。** 破折號很有「AI 感」。無論是網站文案還是給使用者的回覆,能用逗號、句號、括號或直接分句表達的就不要用破折號。

- **每次 push 後,在 `DEVLOG.local.md`(已 ignore)追加一行**:日期、commit hash、做了什麼,必要時補一句為什麼。只記決策摘要,不記對話逐字稿(完整對話 Claude Code 已存在本機 `~/.claude/projects/` 下)。
